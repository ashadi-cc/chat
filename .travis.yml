language: go

go:
  - 1.12.x

before_install:
  - cp .netrc ~/.netrc
  - chmod 600 ~/.netrc

script:
  - go test -v ./...

deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master

services:
  - docker